{
  "name": "root",
  "private": true,
  "scripts": {
    "server": "npm --prefix backend run dev",
    "dev": "npm --prefix frontend run dev",
    "all": "npm-run-all --parallel server dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
